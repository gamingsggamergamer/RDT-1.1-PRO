services:
  - type: web
    name: roblox-llm-level2
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.server:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
